# Goal
status: done
Completion: Roadmap stage 10.3 is locally accepted on one fixed candidate. `HTTP_IDEMPOTENCY=none|postgres` is a default-off initializer profile. `postgres` output retains a complete, documented idempotency pack that stays inert until an operation opts in and meets the Specification on real PostgreSQL. Every `none` output contains no pack path or marker, and its generated contract and projected `Cargo.lock` equal the baseline `d24d173` output for the same selection. The local route that `make plan` selects for the candidate (formatting, workspace lint, build, and tests are among its steps), the source database suite, the 128 canonical projections, the 16 runtime graphs (each initialized, built, and tested once), the one-shot `none` equality, the other named checks, and one independent final delivery review all pass. No PR, push, merge, CI, publication, or deployment result is included.
Global constraints: The [Intent](intent.md), [Specification](spec.md), [system design](design/system.md), [ownership map](design/ownership.md), and the twelve fixed decisions in the [Technical Design Transition](technical-design-transition.md) govern execution; changing one reopens its named owner. The [Planning Ledger Contract](../../docs/spec-first-workflow/phases/planning/ledger-contract.md) and the [validation budget](../../AGENTS.md#validation-budget) govern timing: tests are written with the code, checks and the final review run only at the assembled final-validation boundary, and each heavy run executes once. Authority is local only: no commit, PR, push, merge, publication, or deployment; unrelated work is preserved; other stage-10 capabilities stay out of scope. The workstation has 10 CPUs and 16 GB RAM, so Cargo, initializer, and Docker-backed commands never run concurrently; code-writing lanes may run in parallel only with disjoint writers and locks. Execution evidence (candidate fingerprints, logs, receipts, review records, and the Completion result) lives under `.git/claude/http-idempotency/` as the packet describes; only task artifacts live in this bundle. Any delegated agent that may make a network request receives this rule: put no personal data in it (no email in a User-Agent, URL, header, or payload), and use a neutral User-Agent such as `rust-service-template-rest-research`.

## Tasks

- [x] T1: A derived service can select a complete, documented, inert-until-opt-in PostgreSQL-backed HTTP idempotency profile, while default output keeps the baseline contract and lock.
  - Depends on: none for implementation; the ready Definition and Technical Design close every consumed decision. Final acceptance consumes a usable local Docker daemon (the source database suite and graphs 13–16) and the local baseline commit `d24d173` (the one-shot `none` equality).
  - Provides: the retained pack (the store crate, the `infra_http::idempotency` seam and catalog codes, the retention setting, composition-root and bootstrap wiring, the profile migration, inline and real-PostgreSQL tests, and the adopter guide with its documentation companions) and its profile machinery (selection, lock, inventory, sync, the 128/16 validation runner, and CI and classifier routing); unverified until final validation.
  - Packet: [T1 — PostgreSQL-backed HTTP idempotency profile](tasks/T1-http-idempotency-profile.md)
  - Implementation: `Implemented` on HEAD `ac6a6ffe248cf6dac5232438876c564030316ed1` plus the 63-path working-tree candidate (manifest `.git/claude/http-idempotency/implementation/candidate-manifest.txt`, fingerprint `e91c6ed153f5512002643d12a78ba94f657c33e2763d8f1b961b38fd3f299e41`, verified against the tree by the Orchestrator); all lanes joined. This checkbox records code completion only; verification is pending final validation.
  - Completion: locally `Accepted` for the fixed candidate C6, HEAD `ac6a6ffe248cf6dac5232438876c564030316ed1` plus the uncommitted 63-path working tree (manifest `.git/claude/http-idempotency/delivery/candidate-manifest-c6.txt`, fingerprint `9f6d138f703a58c92dec3f43f409011c8db1f518be2e0d023d27b10b685f1fd3`; C6 is the validated and reviewed C5 `8a02be6413f61a645f079f786eaed395443636f26a492626ba06ce1f344e32bb` plus the roadmap bookkeeping). Evidence: all 18 local steps of the `make plan` route (403 workspace tests, 0 failed), secret scan, runner self-test, `ALLOW_HEAVY=1 make test-integration-db` (P1–P8, P9, and the existing PostgreSQL suite, none skipped), `ALLOW_FULL=1 make template-init-check` receipt `attempt.h7Zin2` (128 projections, 16 runtime graphs each initialized, built, and tested once), and the one-shot `none` equality (24 of 24 digests equal `d24d173`). The earlier failed matrix attempt `attempt.fLY5tm` remains recorded as failed. Independent final review PASS after one repaired finding; FOLLOW_UP FU1 (an unmarked tokio `net` dev-dependency feature also appears in `none` outputs, with no lock or contract effect) is open and non-blocking. Record: `.git/claude/http-idempotency/delivery/completion.md`. Pending CI and not claimed: the runtime image build, `make migration-validate`, `make container-security`, and every CI, PR, publication, or deployment result.
  - Cleanup: deferred to the delivery request. Nothing is committed, so deleting this bundle now would destroy its only copy; the delivery change commits the bundle with the implementation and then retains decisions and closes the bundle, as stage 10.2 did.
