# Technical Design transition

status: ready

owner: Technical Design (System / Integration and Rust Code / Ownership)

result: [design/system.md](design/system.md), [design/ownership.md](design/ownership.md)

review: [technical-design-review.md](technical-design-review.md), PASS after one bounded repair/recheck; [ownership panel](design/ownership-review.md), three independent PASS lenses plus fresh PASS on the related crypto dependency delta.

movement_evidence: Every material flow has a selected mechanism, enforcing owner, error/deadline/lifecycle outcome and proof boundary. Definition's nbf:null contradiction was narrowly reopened and resolved through its fresh reviewed receipt. Final auth derives policy from the complete assembled document with actual-method provenance; explicit undocumented HEAD cannot inherit GET. Bootstrap retains auth without idempotency and enforces auth before key admission. One adapter URL grammar avoids config-provider dependency inversion; reqwest pooling removes only auth DNS. Key material/algorithm eligibility and process-owned refresh publication are closed. Exported contracts and inverse file ownership are sufficient for a fresh Planning actor without inventing architecture. Only task design/review/transition artifacts changed; no implementation or runtime result is claimed.

reopen_owner: none for movement. Specification owns caller behavior changes; System Design owns a real bypass, unsupported library mechanism, changed lifecycle or input boundary; Rust Ownership owns a new semantic file/dependency owner. Executor-local tests and routine helper bodies do not require reopening.

next_owner: Planning, fresh actor dispatched by root. Stop this actor before Planning.

## Candidate relocation and delivery authority

Source base is `4edd184ea3cc6b6fa2b225244700fce57150ae18`. An unrelated actor changed the original checkout branch label during this phase; production source remained unchanged and reviewers bound their claims to fixed file hashes plus base. Root created the isolated `/Users/daniil/Projects/Opensource/rust-service-template-rest.codex-authn-contract-and-libraries` on `codex/authn-contract-and-libraries`. After this actor's final write, root owns copying only `specs/authn-simplification` there and checking unchanged source/instructions and artifact identities before dispatch. Do not import unrelated outbound work.

The user-authorized end-to-end request continues through Planning/Implementation and one separate PR; this phase boundary does not ask for technical reconfirmation. No merge, deployment, live-provider certification, extra database environment or independent Stage10.2 policy change is authorized here. Planning may group work against the exported contracts; avoid concurrent writers in shared lib.rs/bootstrap. Final assembled validation and authorization/lifecycle review remain the delivery owner's responsibility. Keep expensive runtime proof separate from structural profile checks; CI owns its selected remote matrix.

## Implementation carries

The JWT engine adds one direct dependency edge to already-locked aws-lc-rs1.18.1/default-features off solely for supported material import, with JWT-only marker containment. No package/version upgrade or alternate crypto verifier is selected. The existing library still owns signature/registered-claim validation. Optional profile projection must remove the direct edge where JWT is absent, while TLS may retain its own transitive backend.

Documentation and contract tests must reflect effective global security inheritance and the small routes macro/carrier migration. Existing generic pooled-dependency guidance does not create an auth health probe: accepted auth outage remains request-local. If affected skills/instructions change, their current maintenance owners and check-instructions apply. Tests/cases/commands are selected by executors, with no separate test-design gate.

Static documentation verification: final `make docs-check` passed after these receipts were written (exit 0, 799 links checked, zero errors). The owner then replaced this sentence with the result; no link or reviewed design changed. Production source diff remained empty.
