# Stable rustfmt options only; nightly-only options are rejected by `cargo fmt --check` on the pinned toolchain.
edition = "2024"
style_edition = "2024"
newline_style = "Unix"
use_field_init_shorthand = true
use_try_shorthand = true
