# Development and integration-only JetStream server. Production operators own
# replica topology and the durable storage policy described in durable-messaging.
port: 4222
http_port: 8222

jetstream {
  store_dir: "/data/jetstream"
}
